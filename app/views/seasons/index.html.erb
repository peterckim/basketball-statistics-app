<div class="container">
    <h1 class="display-3">Seasons</h1>
    <ul class="list-group list-group-flush">
        <% @seasons.each do |season| %>
            <li class="list-group-item"><%= link_to season.year, season_players_path(season) %></li>
        <% end %>
    </ul>
</div>